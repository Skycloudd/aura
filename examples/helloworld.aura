fn test(a: i32, b: i32, c: i32) {
    {
        1 + 2;
        3 + 4;
    }

    let result = a + b * c;

    return result;
}

fn count(num: i32) {
    queue num;
    queue num + 1;
    queue num + 2;

    return;
}

fn main() {
    test();
    count(3);

    if (1 == 2) {
        1 + 2;
    } else if 3 == 4 {
        3 + 4;
    }

    if (1 != 2)
        1 - 2;
    else if (3 != 4)
        3 - 4;
}
