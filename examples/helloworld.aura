fn add(a, b)
    return a + b;

fn mul(a, b)
    return a * b;

fn test_func(a, b, c)
    # perform some random computation to test things
    return mul(add(a, b), c);

fn main() {
    a = 1;
    b = 2;
    c = 3;

    result = test_func(a, b, c);
}
